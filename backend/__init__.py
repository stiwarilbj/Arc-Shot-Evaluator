"""ARC local basketball shot evaluator.

The package is grouped by responsibility:

* ``api`` exposes the local web server and analysis queue.
* ``analysis`` owns vision, tracking, measurement, and video rendering.
* ``coaching`` turns reliable measurements into grounded practice notes.
* ``domain`` contains the data objects shared by those features.
"""

__version__ = "1.0.0"
